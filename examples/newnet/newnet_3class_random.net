# Randomly initialized NEWNET for 3-class one-hot inputs
# The loader will create neurons and stochastic connectivity/weights.

NEWNET S=3 H=12 O=3 POOL=0

# Optional connectivity densities (defaults shown)
DENSITY S->H 0.6
DENSITY S->O 0.7
DENSITY H->H 0.2
DENSITY H->O 0.9

# Initialization (he | xavier), sign balance and global scale
INIT he
EXCIT_RATIO 0.7
W_SCALE 3.0

# Neuron parameters (thresholds and leak per group)
THRESHOLDS S 100 H 45 O 20
LEAK S 1.0 H 0.9 O 1.0
